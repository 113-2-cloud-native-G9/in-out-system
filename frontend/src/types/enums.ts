export enum LateArrivalStatus {
    OnTime = "On time",
    Late = "Late",
}

export enum EarlyDepartureStatus {
    OnTime = "On time",
    Early = "Early",
}

export enum AttendanceStatus {
    All = "All",
    OnTime = "On time",
    Late = "Late",
    Early = "Early departure",
}
